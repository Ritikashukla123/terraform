---
- name: Playbook
  hosts: localhost
  tasks:
    - name: Install python3-apt package
      yum:
        name: python3-apt
        state: present
    - name: Install Nginx
      yum:
        upgrade: yes
        update_cache: yes
    


