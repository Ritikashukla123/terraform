version: v1.0
name: Terraform Deployment

agent:
  machine:
    type: e1-standard-2

blocks:
  - name: Deploy Infrastructure
    task:
      jobs:
        - name: Terraform Init
          commands:
            - terraform init
        - name: Terraform Apply
          commands:
            - terraform apply -auto-approve

